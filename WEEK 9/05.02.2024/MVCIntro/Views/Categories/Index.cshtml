@* @model List<Category> *@
@* @model CategoryVm *@
@model Tuple<List<Category>, string,DateTime,List<Category>,string>;

<h1 class="display-1">@TempData["tData"]</h1>

@* <h1 class="display-1">@Model.Description</h1> *@

<h1 class="display-1">@Model.Item2</h1>
<h1 class="display-1">@Model.Item3</h1>
<h1 class="display-1">@Model.Item5</h1>
<div class="row">
    <div class="col-6 p-3 rounded shadow-sm">
        <table class="table table-hover">
            <caption>Data transfer to view</caption>
            <thead>
            <tr>
                <th>Kategori ID</th>
                <th> Kategori adi</th>
                <th>Aciklama</th>
            </tr>
            </thead>
            <tbody>
            @* @foreach (var item in Model) *@
            @* @foreach (var item in Model.Categories) *@
            @foreach (var item in Model.Item1)
            {
                <tr>
                    <td>@item.CategoryID</td>
                    <td>@item.CategoryName</td>
                    <td>@item.Description</td>
                </tr>
            }
            </tbody>
        </table>
    </div>

<div class="col-6 p-3 rounded shadow-sm">
    <table class="table table-hover">
        <caption>@ViewData["ViewDataDescription"]</caption>
        <thead>
        <tr>
            <th>Kategori ID</th>
            <th> Kategori adi</th>
            <th>Aciklama</th>
        </tr>
        </thead>
        <tbody>
        @foreach (var item in (ViewData["CategoryListViewData"] as List<Category>)!)
        {
            <tr>
                <td>@item.CategoryID</td>
                <td>@item.CategoryName</td>
                <td>@item.Description</td>
            </tr>
        }
        </tbody>
    </table>
</div>


<div class="col-6 p-3 rounded shadow-sm">
    <table class="table table-hover">
        <caption>@ViewBag.ViewBagDescription</caption>
        <thead>
        <tr>
            <th>Kategori ID</th>
            <th> Kategori adi</th>
            <th>Aciklama</th>
        </tr>
        </thead>
        <tbody>
        @foreach (var item in ViewBag.CategoryListViewBag)
        {
            <tr>
                <td>@item.CategoryID</td>
                <td>@item.CategoryName</td>
                <td>@item.Description</td>
            </tr>
        }
        </tbody>
    </table>
</div>

</div>
